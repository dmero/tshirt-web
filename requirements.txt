# Django Framework
Django==5.2.17

# Database Driver
# Install requirements-mysql.txt only when retaining the existing MySQL database.
dj-database-url==3.1.2
psycopg[binary]==3.3.5
whitenoise==6.12.0
gunicorn>=23,<24; sys_platform != 'win32'
django-storages[s3]==1.14.6

# Image Processing (for ImageField)
Pillow==12.3.0

# Payment Processing
stripe==7.14.0

# Optional: For better .env handling (alternative to custom loader)
# python-decouple==3.8

# Development Dependencies
# django-debug-toolbar==4.2.0
